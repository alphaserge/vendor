import * as React from 'react';

import ExpandMoreIcon from '@mui/icons-material/ExpandMore';

export default function ExpandDown(props) {
  return <ExpandMoreIcon sx={{fontSize: "13px", color: "#fff"}} />
         
}
